PORT=3000
ADMIN_PASSWORD=change-me
SESSION_SECRET=change-me-too

# Optional -- only needed to stop players.json/accounts.json/activity.json/
# usedThemes.json from resetting on Render's free plan (no persistent disk).
# Leave blank for local dev -- your local disk is already persistent, this
# does nothing there. See README.md "Чому зникають акаунти на Render".
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
